ifdef::env-github,rspecator-view[]

'''
== Implementation Specification
(visible only on this page)

== Message

When a dangerous wildcard is found:

* Overly-wide glob copies might inadvertently add sensitive data to the container. Make sure it is safe here.

In any other case:

* Overly-wide recursive copies might inadvertently add sensitive data to the container. Make sure it is safe here.

== Highlighting

The `COPY` or `ADD` dangerous source.

'''

endif::env-github,rspecator-view[]
