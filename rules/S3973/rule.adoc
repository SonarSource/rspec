== Why is this an issue?

include::description.adoc[]

[source,java]
----
if (condition)  // Noncompliant
doTheThing();
doTheOtherThing(); // Did we really intend to always call this function?
----

Indentation makes the intent clear.

[source,java]
----
if (condition)
  doTheThing();
doTheOtherThing(); // We clearly intended to always call this function.
----

