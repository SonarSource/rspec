Calling the ``++!++`` or ``++~++`` prefix operator twice does nothing: the second invocation undoes the first. Such repetitions are usually typos.

include::{example}[]

Either this is a bug if the operator was meant to be called once, or misleading if done on purpose.
