Any class that doesn't directly extend <code>Object</code> should invoke <code>super</code> in the constructor that performs initialization to ensure that all parts of the class are fully initialized.
