== Why is this an issue?

include::../description.adoc[]


Example: Use `.Capabilities.KubeVersion.Version` instead of the deprecated call to `.Capabilities.KubeVersion.GitVersion`

[source,text]
----
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
data:
  myvalue: "Hello World"
  kubeVersion: {{ .Capabilities.KubeVersion.GitVersion }} # Noncompliant

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
data:
  myvalue: "Hello World"
  kubeVersion: {{ .Capabilities.KubeVersion.Version }}
----

include::../see.adoc[]

* Go Documentation - https://pkg.go.dev/helm.sh/helm/v3/pkg/chartutil#KubeVersion.GitVersion[Deprecated KubeVersion.GitVersion]

ifdef::env-github,rspecator-view[]

'''
== Implementation Specification
(visible only on this page)

=== Message

Show the message added along with commented tag for the deprecated code. For the above example - Deprecated: use KubeVersion.Version.

=== Highlighting

* Highlight the code that is deprecated
endif::env-github,rspecator-view[]