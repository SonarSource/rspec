=== Message

- Make sure that disabling Azure Managed Identities by not specifying `identity` is safe here.

Only for Azure Data Factory

- Make sure that disabling Azure Managed Identities is safe here.
