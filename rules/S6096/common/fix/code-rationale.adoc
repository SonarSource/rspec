The following code is vulnerable to Zip Slip as it is constructing a path using untrusted data. This path is then used to copy a file without being validated first. Therefore, it can be leveraged by an attacker to overwrite arbitrary files.