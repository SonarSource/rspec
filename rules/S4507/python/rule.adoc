include::../description.adoc[]

include::../ask-yourself.adoc[]

include::../recommended.adoc[]

== Sensitive Code Example

Django application startup:

[source,python,diff-id=1,diff-type=noncompliant]
----
from django.conf import settings

settings.configure(DEBUG=True)  # Sensitive when set to True
settings.configure(DEBUG_PROPAGATE_EXCEPTIONS=True)  # Sensitive when set to True

def custom_config(config):
    settings.configure(default_settings=config, DEBUG=True)  # Sensitive
----

Django's "settings.py" or "global_settings.py" configuration file:

[source,python,diff-id=2,diff-type=noncompliant]
----
# NOTE: The following code raises issues only if the file is named "settings.py" or "global_settings.py". This is the default
# name of Django configuration file

DEBUG = True  # Sensitive
DEBUG_PROPAGATE_EXCEPTIONS = True  # Sensitive
----

Flask application startup:

[source,python,diff-id=3,diff-type=noncompliant]
----
from flask import Flask

app = Flask()
app.debug = True  # Sensitive
app.config['DEBUG'] = True  # Sensitive
app.run(debug=True)  # Sensitive
----

== Compliant Solution

[source,python,diff-id=1,diff-type=compliant]
----
from django.conf import settings

settings.configure(DEBUG=False)  # Compliant
settings.configure(DEBUG_PROPAGATE_EXCEPTIONS=False)  # Compliant

def custom_config(config):
    settings.configure(default_settings=config, DEBUG=False)  # Compliant
----

[source,python,diff-id=2,diff-type=compliant]
----
# NOTE: The following code raises issues only if the file is named "settings.py" or "global_settings.py". This is the default
# name of Django configuration file

DEBUG = False  # Compliant
DEBUG_PROPAGATE_EXCEPTIONS = False  # Compliant
----

[source,python,diff-id=3,diff-type=compliant]
----
from flask import Flask

app = Flask()
app.debug = False  # Compliant
app.config['DEBUG'] = False  # Compliant
app.run(debug=False)  # Compliant
----

include::../see.adoc[]

ifdef::env-github,rspecator-view[]

'''
== Implementation Specification
(visible only on this page)

include::../message.adoc[]

'''
== Comments And Links
(visible only on this page)

include::../comments-and-links.adoc[]

endif::env-github,rspecator-view[]
