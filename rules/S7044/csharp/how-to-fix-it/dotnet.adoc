== How to fix it in ASP.NET

=== Code examples

include::../../common/fix/code-rationale.adoc[]

==== Noncompliant code example

[source,csharp,diff-id=1,diff-type=noncompliant]
----
using System.Web;
using System.Web.Mvc;

public class ExampleController: Controller
{
    [HttpGet]
    public IActionResult GetUser(string id)
    {
        string base = "http://example.com/api/user/";
        HttpWebRequest request = (HttpWebRequest)WebRequest.Create(base + id); // Noncompliant

        return Ok();
    }
}
----

==== Compliant solution

[source,csharp,diff-id=1,diff-type=compliant]
----
using System.Web;
using System.Web.Mvc;

public class ExampleController: Controller
{
    [HttpGet]
    public IActionResult GetUser(string id)
    {
        string base = "http://example.com/api/user/";
        HttpWebRequest request = (HttpWebRequest)WebRequest.Create(base + HttpUtility.UrlEncode(id));

        return Ok();
    }
}
----

=== How does this work?

include::../../common/fix/encoding.adoc[]
