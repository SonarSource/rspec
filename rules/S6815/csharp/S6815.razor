@* Issue will be raise at the file level, some adjustements for UT will be required *@
@* Noncompliant@+1 *@
@page "/route/{*catchAllRoute}/another/segment"
@* Compliant@+1 *@
@page "/route/another/segment/{*catchAllRoute}"

@code {
    [Parameter]
    public string CatchAllRoute { get; set; }
}