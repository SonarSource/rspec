ifdef::env-github,rspecator-view[]

'''
== Implementation Specification
(visible only on this page)

=== Message

* Omitting `--ignore-scripts` can lead to the execution of external shell scripts. Make sure it is safe here.

=== Highlighting

Highlight `npm install` or `yarn install` shell command.

'''

endif::env-github,rspecator-view[]
