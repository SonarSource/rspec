When working with `{framework}`, to fix this issue, it is important that all test classes annotated with the `[{classAnnotation}]` attribute contains at least one method with the one of the following method attributes: