== Compliant Solution

[source,csharp]
----
int? nullable = null;
...
if (nullable.HasValue)
{
  UseValue(nullable.Value);
  // or
  UseValue((int)nullable);
}
----
or

[source,csharp]
----
int? nullable = null;
...
if (nullable != null)
{
  UseValue(nullable.Value);
  // or
  UseValue((int)nullable);
}
----
