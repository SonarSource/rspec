== Compliant Solution

[source,csharp]
----
int? nullable = null;
...
if (nullable.HasValue)
{
  Console.WriteLine(nullable.Value);
  // or
  Console.WriteLine((int)nullable);
}
----
or

[source,csharp]
----
int? nullable = null;
...
if (nullable != null)
{
  Console.WriteLine(nullable.Value);
  // or
  Console.WriteLine((int)nullable);
}
----
