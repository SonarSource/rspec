[source,java]
----
public String readFile(File f) {
  return readFromDisk(f);
}
----

or

[source,java]
----
public String readFile(File f) {
  String content;
  try {
    content = readFromDisk(f);
  } catch (IOException e) {
    logger.LogError(e);
    throw e;
  }
  return content;
}
----